/*
package com.hassan.gestiondestock.Mapper;

import com.hassan.gestiondestock.dto.EntrepriseDto;
import com.hassan.gestiondestock.model.Entreprise;
import org.mapstruct.Mapper;

import java.util.List;
import java.util.Optional;

@Mapper(componentModel = "spring", uses = {
        AdressMapper.class
})
public interface EntrepriseMapper {

    EntrepriseDto toDto(Entreprise model);
    EntrepriseDto toDto(Optional<Entreprise> model);
    Entreprise   toModel(EntrepriseDto dto);
    List<EntrepriseDto> toDtos(List<Entreprise> model);
    List<Entreprise>   toModels(List<EntrepriseDto> dto);
}
*/
