/*
package com.hassan.gestiondestock.Mapper;

import com.hassan.gestiondestock.dto.ventesDto;
import com.hassan.gestiondestock.model.Ventes;
import org.mapstruct.Mapper;

import java.util.List;
import java.util.Optional;

@Mapper(componentModel = "spring", uses = {
        LigneVenteMapper.class
})
public interface VentesMapper {

    ventesDto toDto(Ventes model);
    ventesDto toDto(Optional<Ventes> model);
    Ventes   toModel(ventesDto dto);
    List<ventesDto> toDtos(List<Ventes> model);
    List<Ventes>   toModels(List<ventesDto> dto);

}
*/
