/*
package com.hassan.gestiondestock.Mapper;

import com.hassan.gestiondestock.dto.utilisateursDto;
import com.hassan.gestiondestock.model.Utilisateurs;
import org.mapstruct.Mapper;

import java.util.List;
import java.util.Optional;

@Mapper(componentModel = "spring", uses = {
        AdressMapper.class,InformationMapper.class
})

public interface UtilisateurMapper {

    utilisateursDto toDto(Utilisateurs model);
    utilisateursDto toDto(Optional<Utilisateurs> model);
    Utilisateurs   toModel(utilisateursDto dto);
    List<utilisateursDto> toDtos(List<Utilisateurs> model);
    List<Utilisateurs>   toModels(List<utilisateursDto> dto);
}
*/
