/*
package com.hassan.gestiondestock.Mapper;

import com.hassan.gestiondestock.dto.clientsDto;
import com.hassan.gestiondestock.model.clients;
import org.mapstruct.Mapper;

import java.util.List;
import java.util.Optional;

@Mapper(componentModel = "spring", uses = {
        AdressMapper.class,InformationMapper.class
})
public interface ClientMapper {

    clientsDto toDto(clients model);
    clients   toModel(clientsDto dto);
    List<clientsDto> toDtos(List<clients> model);
    List<clients>   toModels(List<clientsDto> dto);
    clientsDto toDto(Optional<clients> model);

}
*/
